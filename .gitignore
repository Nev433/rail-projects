# macOS
.DS_Store
.AppleDouble
.LSOverride

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build outputs
dist/
build/
out/
.next/
.angular/
.nuxt/
.svelte-kit/
.cache/
*.tsbuildinfo

# .NET
bin/
obj/
*.user
*.suo

# Test / coverage
coverage/
.nyc_output/
*.lcov

# Env files (commit *.example, never the real thing)
.env
.env.local
.env.*.local

# Editor / IDE local state
.idea/
*.swp
*.swo

# Logs
*.log

# OS / misc
Thumbs.db

# Standards: bulk upstream artefacts (regenerable from source distribution).
# Scaffolding (README/MANIFEST/CHANGELOG/SOURCE), schemas and codelists are
# tracked; generated docs and model files are not.
standards/**/documentation/
standards/**/source/model/

# Legacy projects pending migration into the new Nest+Angular approach.
# Excluded from git tracking; revisit per-project as each is brought over.
ToDo/
